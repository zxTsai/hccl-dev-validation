name: sendrecv_2p_small
category: primitives
op: SendRecv
rank_size: 2
dtype: fp32
count: 4096
scale: small
backend: hccl_vm
enabled: false
backend_config:
  case_config: case_configs/primitives/sendrecv_2p_small_vm.json
  timeout_sec: 180
expect:
  returncode: 0
  required_log_patterns: []
  forbidden_log_patterns:
  - '[ERROR]'
  - execute failed
  - 'RESULT: FAILED'
  - Segmentation fault
  - core dumped
  - comm is nullptr
notes:
  method: SendRecv placeholder case. Current CANN hccl_test/bin does not provide a
    confirmed send/recv or sendrecv test binary.
  expected_behavior: Keep this case skipped until a supported SendRecv test executable
    and stable arguments are found.
  check_method: Disabled case is reported as SKIP with this reason; it must not be
    counted as a real PASS.
  reason: No confirmed send/recv or sendrecv test binary exists under the current
    CANN hccl_test/bin directory.
